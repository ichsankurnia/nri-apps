export interface Album {
    userId?: number
    id: string
    title: string
}

export interface AlbumList {
    albumId?: string
    id?: string
    title?: string
    url?: string
    thumbnailUrl?: string
}